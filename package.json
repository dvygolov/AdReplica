{
  "name": "adreplica",
  "version": "140526b4",
  "private": true,
  "description": "Browser-side Facebook Ads Manager export, import, clone, and bookmarklet loader tooling.",
  "scripts": {
    "check": "node --check adreplica-og-packager.js && node --check adreplica.js && node --check adreplica-loader.js",
    "build": "node adreplica-og-packager.js --base-url=https://adreplica.pages.dev/adreplica",
    "deploy": "npx wrangler pages deploy ./dist --project-name=adreplica --branch=main --commit-dirty=true"
  },
  "keywords": [
    "facebook-ads",
    "ads-manager",
    "bookmarklet",
    "campaign-clone"
  ],
  "author": "Yellow Web",
  "license": "UNLICENSED",
  "devDependencies": {}
}
